import { Blocks, Gauge, PenTool, ScanSearch, Search, type LucideIcon } from 'lucide-react';

/** Iconos de las cinco etapas, en el mismo orden que `t.process.steps`. */
export const processIcons: LucideIcon[] = [Search, ScanSearch, PenTool, Blocks, Gauge];
