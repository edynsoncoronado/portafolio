services:
  # qdrant:
  #   image: qdrant/qdrant:v1.12.1
  #   container_name: ml_qdrant
  #   ports:
  #     - "6333:6333"
  #   volumes:
  #     - qdrant-data:/qdrant/storage

  ml-env:
    build:
      context: .
      dockerfile: ./Dockerfile
    working_dir: /app
    volumes:
      - ../app:/app
    container_name: ml-env-dev
    # depends_on:
    #   - qdrant
    tty: true

# volumes:
#   qdrant-data:
